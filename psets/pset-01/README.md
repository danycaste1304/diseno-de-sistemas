# PSet 01 — ReservaU

Implementación orientada a objetos del modelo de dominio de ReservaU.

Para ejecutar la simulación desde `implementacion/`:

```bash
python simulacion.py
```

O con Docker:

```bash
docker build -t reservau .
docker run --rm reservau
```
